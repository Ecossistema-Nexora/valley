apiVersion: v1
kind: Service
metadata:
  name: {{ include "valley.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "valley.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
