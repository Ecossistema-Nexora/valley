image:
  repository: 123456789012.dkr.ecr.us-east-1.amazonaws.com/valley-prod/valley
  tag: sha-CHANGE_ME

worker:
  enabled: false

cron:
  enabled: false

ingress:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: valley.example.com
  hosts:
    - host: valley.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: valley-tls
      hosts:
        - valley.example.com
