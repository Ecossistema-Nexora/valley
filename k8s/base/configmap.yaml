apiVersion: v1
kind: ConfigMap
metadata:
  name: valley-config
  namespace: valley
data:
  NODE_ENV: "production"
  PORT: "8080"
  MULTI_TENANT_MODE: "true"
  STRIPE_BILLING_ENABLED: "false"
  APP_BASE_URL: "https://valley.example.com"
