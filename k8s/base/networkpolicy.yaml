apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: valley-default
  namespace: valley
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector: {}
  egress:
    - to:
        - namespaceSelector: {}
